{
  "status": "active",
  "version": "1.0",
  "bug_type": "schema_corruption",
  "activation_date": "2024-03-25",
  "target_table": "users",
  "sql_commands": [
    "ALTER TABLE users MODIFY id INT NOT NULL",
    "ALTER TABLE users DROP PRIMARY KEY",
    "ALTER TABLE users ADD COLUMN temp_bug_flag INT DEFAULT 1"
  ],
  "rollback_sql": [
    "ALTER TABLE users DROP COLUMN temp_bug_flag",
    "ALTER TABLE users MODIFY id INT NOT NULL AUTO_INCREMENT",
    "ALTER TABLE users ADD PRIMARY KEY (id)"
  ],
  "instructions": "System maintenance in progress. Run the provided SQL commands."
}
